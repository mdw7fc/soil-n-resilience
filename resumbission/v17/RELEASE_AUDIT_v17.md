# v17 release audit

Status: **PASS** — seventh pass: F-031 bounds the four-pool mechanism
attribution (the fixed-CUE analysis assigns only 6-59% of the positive
4-pool-Century loss difference to CUE downregulation; single-mechanism
causal language is withdrawn from Note 2, the table 2 note, the Figure S5
caption and the manuscript Discussion), corrects the four-pool code's
opening description, gives the 22 four-pool ledger rows real units and an
honest uncertainty-treatment column, and fixes the subscript formatting of
the four-pool overview paragraph on SI page S-5. FINDINGS.md F-028 through
F-031 record the rounds. The release gate reads the released files
themselves and now also reads the repository's own documentation, so this
document's claims are enforced, not asserted.

State at release commit:

- `make verify`: 18 analytical test suites and the 33-node build graph,
  exit 0, zero blocked nodes, zero orphans, zero unsourced inputs and zero
  unstamped baselines. The whole graph, Monte Carlo ensemble included, was
  regenerated at this commit (1,000 draws, seeded, through the build graph).
- Build outputs are canonicalized at write time (F-028): float literals in
  .json/.csv/.csv.gz node outputs carry at most six significant digits, JSON
  is dumped in one canonical form, gzip containers use mtime=0. Five nodes
  rerun after the regeneration reproduced their outputs byte-identically in
  this container; the third audit's cross-machine staleness failure was
  1e-14-scale float noise, which this rounding absorbs by construction.
  Figure PDFs and PNGs remain renderer-dependent in principle; the ten
  doc-embedded PNGs are hash-checked against the deposited renders.
- Claim register `docs/claims.yaml` v17-f031, document basis v17: 20 claims,
  78 checks, 78 AGREES, 0 DRIFTED. The legacy pre-rebuild register is
  archived under `docs/archive/`; README, MANIFEST, HANDOFF and CHANGELOG
  are gated by the new repo-docs suite (released family required, stale
  family forbidden, working records must carry a SUPERSEDED banner).
- The cross-document consistency test reads the v17 clean files in this
  repository, requires the corrected values and the reinstatement/net-revenue
  fragments, forbids the known-stale fragments and all version-referential
  language in the published documents, and hash-checks ten
  embedded figures against the deposited renders.
- Clean files are produced by an XML-level accept that preserves equation
  objects (tracked and clean SI both carry 55 oMath + 24 oMathPara
  elements); zero revision markup and zero comments in all three clean
  files; all six documents pass OOXML schema validation. Locally rendered
  PDFs blank the equations; that is a LibreOffice rendering limitation, and
  the final visual check in Word is the one step this environment cannot
  perform.

Central results (eps_F_N = -0.50 central, realized-yield clearing):

- Global S3 loss 2.32 / 3.02 / 3.07 % at years 1/10/30; regional year-10
  range 1.2% (East Asia) to 5.1% (FSU/Central Asia), South Asia 4.8%, SSA
  4.7%. Year-10 food-price response 2.6% to 11.9%, global 6.7%.
- Realized S3 fertilizer reduction 18.7% N-tonnage-weighted (~19%; +104%
  calibrated shock), regenerated by the s3_shock_calibration node that
  retired the last unsourced input.
- Net revenue at mean SOC under a 100% spike: SSA +0.0%, LATAM -1.5%, NA
  -1.0%, South Asia -6.8%; half-to-mean retains 0.3-1.0 pp; the
  SSA-largest-financial-loss claim is retracted. All financial statements
  use the partial net-revenue framing (crop revenue net of
  nitrogen-fertilizer expenditure, four priced regions); no gross-margin
  claim survives in any document.
- Ensemble: yield buffering universal (P = 1.0 in every region) and
  net-revenue buffering P = 1.0 in each of the four priced regions; median
  buffer 0.88 pp, global median year-1 loss 2.51%, SSA worst-net-revenue in
  none of the 1,000 draws. Note 6 counts 24,000 yield evaluations plus
  12,000 partial net-revenue evaluations.
- The four-pool engine comparison is in the deposit (F-029) and framed as
  EXPLORATORY structural sensitivity (F-030): the scheme lives in
  som_4pool_monthly.py + coupled_4pool.py (node fourpool_comparison) and
  runs the same ERA5 forcing, realized clearing, water-stress response and
  production-path calibration as the Century engine. Year-30 SOC-loss
  ratios 0.9-2.2x Century (median 1.65x; 1.56-1.77 across clay+silt
  0.35-0.75), larger in seven of eight regions and smaller in relative
  terms only in SSA (whose sub-parity ratio partly reflects its larger
  4-pool equilibrium baseline: absolute attributable losses are larger
  under the 4-pool scheme in every region). The pathway description is
  corrected everywhere (roughly half of equilibrium MAOM formation arrives
  by direct DOM sorption, not all through microbial biomass); the engines'
  differing baseline states are disclosed in Note 2 and the unsupported
  'normalization makes the ratio unaffected' claim is withdrawn; all 22
  four-pool parameters are in the parameter ledger and the governing
  equations and values are in the Supplementary Methods; Supplementary
  table 2 is relabeled (it partitions the 4-pool minus Century DIFFERENCE)
  and carries absolute losses next to ratios; Figure S5 reports the
  disruption-attributable carbon budget against a no-shock control (the
  earlier gross-respiration figure conflated the two quantities). Claim
  C-073 pins the family, the texture medians, the sorption share and the
  SSA absolute budget.
- The published documents are STANDALONE: no sentence in the manuscript or
  SI refers to the submitted version or to "this revision" (banned by the
  cross-document gate); revision history lives only in the response
  letter. Note 8 is rewritten as "Model verification and reproducibility";
  the crop calendar is documented in the Supplementary Methods and cited
  (Sacks et al. 2010, ref [41]); the stale figS10-caption values the
  audits had missed (21.0%/9.4%/55%) are corrected to the regenerated
  family (5.3%/4.9%/~8%).
- Tracked changes are word-level: 220+ coarse sentence-replacement pairs
  were collapsed into word diffs (only changed words carry ins/del marks),
  verified byte-equivalent in both the accepted text and the original-view
  text before and after the rewrite.
- The response letter carries the requested verbatim manuscript excerpts
  for Referee 2's four substantive concerns and the revised uncertainty
  paragraph, the corrected elasticity notation, the moderated validation
  language ("evaluated against independent temperate and tropical
  benchmarks"), the crop-calendar answer, and the added
  manuscript-structure paragraph ("Added.").

Reproducibility: `reproducibility/ERFS-100341-soil-resilience_v17_full.bundle`
is SELF-CONTAINED (a single-commit snapshot of the release tree; clone it
directly; the full development history remains in the project's incremental
bundles) and `v17_release_code_docs.zip` is the complete tree at the release
commit, nothing omitted.

Open before submission: Dale Manning's sign-off on the net-revenue reframing
and the response-letter disclosures; the AI-use statement's scope (Claude is
named; Codex assisted the three external audits and the v16_sol line — naming
it is a journal-policy decision); a Word-side page-through of the three clean
files; GitHub push and Zenodo refresh from the release commit.
