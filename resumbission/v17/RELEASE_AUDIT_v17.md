# v17 release audit

Status: **PASS** — third pass, after the second external audit of 2026-08-29.
FINDINGS.md F-027 records the round; the release gate now reads the released
files themselves, so this document's claims are enforced, not asserted.

State at release commit:

- `make verify` from a clean checkout: 17 analytical test suites and the
  32-node build graph, exit 0, with zero blocked nodes, zero orphans, zero
  unsourced inputs and zero unstamped baselines. The Monte Carlo ensemble is
  regenerated and provenance-stamped through the build graph (1,000 draws,
  12.3 minutes; seeded), not carried as a deposit.
- Claim register `docs/claims.yaml` v17-f026, document basis v17: 19 claims,
  70 checks, 70 AGREES, 0 DRIFTED. The generated report states the same
  basis.
- The cross-document consistency test reads the v17 clean files in this
  repository, requires the corrected values (Table S4 y_max, Note 3
  correlations, BNF row, climate 0.62 pp, ensemble statements), forbids
  twenty known-stale fragments, and hash-checks ten embedded figures against
  the deposited renders.
- Clean files are produced by an XML-level accept that preserves equation
  objects: the clean SI carries all 103 OMML equations byte-identical to the
  tracked SI, with zero revision markup and zero comments in all three clean
  files. Locally rendered PDFs blank the equations; that is a LibreOffice
  rendering limitation, and the final visual check in Word is the one step
  this environment cannot perform.

Central results (eps_F_N = -0.50 central, realized-yield clearing):

- Global S3 loss 2.32% / 3.02% / 3.07% at years 1/10/30; regional year-10
  range 1.2% (East Asia) to 5.1% (FSU/Central Asia), South Asia 4.8%, SSA
  4.7%. Year-10 food-price response 2.6% to 11.9%, global 6.7%.
- Realized S3 fertilizer reduction ~19% (18.7% computed, regenerated by the
  new s3_shock_calibration node that retired the last unsourced input).
- Net revenue at mean SOC under a 100% spike: SSA +0.0%, LATAM -1.5%, NA
  -1.0%, South Asia -6.8%; half-to-mean retains 0.3-1.0 pp; the
  SSA-largest-financial-loss claim is retracted.
- Ensemble: buffering universal (P = 1.0 in every region), median buffer
  0.88 pp, global median year-1 loss 2.51%, SSA worst-net-revenue in none of
  the 1,000 draws.
- Figure S5, Supplementary table 2 and the four-pool 0.9x-2.5x ratios are
  withdrawn (tracked deletions with the withdrawal stated in Note 2 and the
  manuscript), pending a regenerable engine.

Reproducibility: `reproducibility/ERFS-100341-soil-resilience_v17_full.bundle`
is SELF-CONTAINED (a single-commit snapshot of the release tree; clone it
directly; the full development history remains in the project's incremental
bundles) and `v17_release_code_docs.zip` is the complete tree at the release
commit, nothing omitted.

Open before submission: Dale Manning's sign-off on the net-revenue reframing
and the response-letter disclosures; the AI-use statement's scope (Claude is
named; Codex assisted the two external audits and the v16_sol line — naming
it is a journal-policy decision); a Word-side page-through of the three clean
files; GitHub push and Zenodo refresh from the release commit.
